# RouteWise Agentic — backend dependencies (Workstream A, Phase A1 foundation)
# Kept minimal and justified per docs/DEVELOPMENT_RULES.md rule 9.

# --- Runtime ---
fastapi>=0.115,<1.0            # web framework / API layer
uvicorn[standard]>=0.32,<1.0   # ASGI server to run FastAPI locally
pydantic>=2.9,<3.0             # data validation / schemas (FastAPI-native)
pydantic-settings>=2.6,<3.0    # environment-based configuration (Settings)
httpx>=0.27,<1.0               # AI (Qwen/Model Studio) HTTP client + FastAPI TestClient

# --- Dev / test ---
pytest>=8.3,<9.0               # backend foundation tests
